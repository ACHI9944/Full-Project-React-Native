import { StyleSheet } from "react-native";

const LoginWithTextStyle = StyleSheet.create({
  image: {
    width: 80,
    height: 80,
  },

  gradientText: {
    fontSize: 50,
    fontFamily: "bona-nova",
  },
});

export default LoginWithTextStyle;
