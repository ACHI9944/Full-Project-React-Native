import { StyleSheet } from "react-native";

const CampaignCreatorsScreenStyle = StyleSheet.create({
  ScrollView: {
    backgroundColor: "white",
    width: "100%",
  },
});

export default CampaignCreatorsScreenStyle;
